/*
 * @author:Lau
 * @version:V1.1 
 * Upgrade_date:2019/4/30
 * update_log:V1.1 Change function;
 *            V1.0 create code.
 * 
 * */

import java.util.Random;
import java.util.Scanner;

public class Pig 	{
	void play(){
		int score_player=0;
		int score_computer=0;
		int count=1;
		while(true){
			System.out.println("第"+count+"回合");
			System.out.println("你的决策是？");
			while(score_player<100){
				//player
				Random rand = new Random();
				int num =rand.nextInt(6)+1;	//生成从1到6的随机数
				if(num==1){
					System.out.println("你的点数为1，当前决策结束");
					break;}
				else {
					System.out.println("你当前掷出的点数为"+num);
					System.out.println("请输入指令：（r：重掷  h:保存得分)");
					Scanner conmmand=new Scanner(System.in);
					String cmd1=conmmand.next();
					char cmd=cmd1.charAt(0);
					if (cmd=='r')continue;
					else if (cmd=='h'){
						score_player+=num;
						break;}
					conmmand.close();}
			}
			int tmp=0;
			int save=0;
			System.out.println("计算机的决策");
			while(score_computer<100){
				//computer
				Random rand2 = new Random();
				int num2 =rand2.nextInt(6)+1;//生成从1到6的随机数
				System.out.println("计算机当前掷出的点数为"+num2);
				if (num2==1) {
					System.out.println("计算机当前决策结束");
					save=0;
					break;}
				else 	{
					tmp+=num2;	
					if(tmp>=20){
						save=1;
						score_computer+=tmp;
						System.out.println("计算机当前掷出的点数累计已满20");
						break;}
				}
				}
				while(save==1) {
					//满20后进行的累加
					Random rand3 = new Random();
					int num3 =rand3.nextInt(6)+1;//生成从1到6的随机数
					System.out.println("计算机当前掷出的点数为"+num3);
					if (num3==1) {
						System.out.println("计算机当前决策结束");
						break;
					}
					else score_computer+=num3;
					if (score_computer>=100) {
						System.out.println("计算机总得分大于100，当前决策结束");
						break;
					}
				}
			System.out.println("你的得分为;"+score_player+",我的得分为："+score_computer);
			if(score_computer>=100||score_player>=100)break;
			count++;}
		if (score_computer>=100)System.out.println("hhh,我赢了！");
		else System.out.println("GDX,你赢了！");}		
}








